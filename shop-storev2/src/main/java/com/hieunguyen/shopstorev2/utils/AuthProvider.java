package com.hieunguyen.shopstorev2.utils;

public enum AuthProvider {
    LOCAL,      // Đăng nhập bằng username/password tự quản
    GOOGLE,     // Đăng nhập qua Google OAuth2
    FACEBOOK    // Đăng nhập qua Facebook OAuth2
}
